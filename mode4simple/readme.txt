This demo showcases basic mode4 usage using a simpler image with a small palette less than 256 colors.

This generates a palette with the exact colors from the image.

The images palette is loaded into palette memory and is drawn on the screen.

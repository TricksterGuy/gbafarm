This demo show cases basic mode3 usage.

The image background.png is resized to 240x160 and is drawn on the screen.

Replacing background.png with your image will resize it to 240x160 
This can be removed in export.sh in that case imageinfo.c needs to be modified with the size of the image.
